import { Embed, EmbedBuilder } from "discord.js";
import { Command } from "../..";

export default {

hidden: true,

Callback({Client,Args,Message}) {
    const embed = new EmbedBuilder()

    
},

} as Command